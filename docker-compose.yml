version: "3.8"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    command: ["poetry", "run", "streamlit", "run", "main.py", "--server.port=8501", "--server.address=0.0.0.0"]
